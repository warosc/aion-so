{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Harlan: Attach to QEMU (LLDB gdbstub)",
      "type": "lldb",
      "request": "custom",
      "targetCreateCommands": [
        "target create ${workspaceFolder}/target/x86_64-unknown-uefi/debug/harlan-boot.efi"
      ],
      "processCreateCommands": ["gdb-remote localhost:1234"],
      "preLaunchTask": "Harlan: Debug QEMU"
    }
  ]
}
